#lang rhombus/static

import:
  file("../main.rhm") open

fun foo(x):
  x + 1

fun bar(x, y):
  x + 2 * y

// def [xs, ...] = [1, 2, 3]
// def [ys, ...] = [2, 4, 6]
// [foo(xs), ...]
// // [bar(xs, ys), ...]
// // [bar(1, ys), ...]


// fun
// | max([n]): n
// | max([n, ns, ...]):
//     let m = max([ns, ...])
//     if m > n | m | n
